import mysql from "../database/mysql-connection";

const db = mysql.connection;

const getPartnerById = async (partnerId: any) => {
    try {

    } catch (e) {

    }
}

export default {
    getPartnerById
}
